# Fibbonacci.pow

calc_fib = func(x)
{
    if(x < 3)
        return 1
    else
        return calc_fib(x - 2) + calc_fib(x - 1)
};

i = 8;
j = calc_fib(i);
message = "Fibbonacci's " + as_str(i) + "(th) number is " + as_str(j);
output(message);