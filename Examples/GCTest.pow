# GCTest.pow

i = 0;
while(1) {
    a = {};
    b = {};
    a.x = b;
    b.x = a;

	output("i = " + as_str(i) + "\n");
	sleep(1);

	i = i + 1;

    if(i > 16) {
        i = 0;
		 output("Collect!\n");
        gc();
    }
}