# Fork.pow

output("Hello!\n");

fork
{
    i = 0;
    while(i < 10)
    {
        output("fork A: " + i + "\n");
        yield;
        i += 1;
    }
}
else
{
    j = 0;
    while(j > -10)
    {
        output("fork B: " + j + "\n");
        yield;
        j -= 1;
    }
};

output("Good-bye!\n");