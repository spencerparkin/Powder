# Fibbonacci.pow

func calc_fib(x)
{
    if(x < 3)
        return 1
    else
        return calc_fib(x - 2) + calc_fib(x - 1)
};

i = 7;
j = calc_fib(i);
message = "Fibbonacci's " + i + "(th) number is " + j;
output(message);