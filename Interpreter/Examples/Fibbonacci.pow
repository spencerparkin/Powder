# Fibbonacci.pow

func calc_fib(i)
{
    if(i < 3)
        return 1;
    else
        return calc_fib(i - 2) + calc_fib(i - 1);
};

i = 5;
j = calc_fib(i);
message = "Fibbonacci's " + i + "(th) number is " + j;
print(message);